# Decisions log

Append-only. One entry per decision; newest at the bottom. Child sessions: log, don't ask.

| ID | Decision | Why | Who |
| --- | --- | --- | --- |
| D-01 | V0 scope = money-depth for PA Senate 2024 (ledger, entities, first-pass chains, preliminary traceability, unmatched ad gallery, dossier skeleton). TX 2026 stubbed. | Chains are the differentiator and share tables with the ledger; dossiers need human verification and are the ideal teammate task Saturday. | Albert + master |
| D-02 | Stack: Python/DuckDB pipeline → static JSON → Next.js 15 (App Router, TS, Tailwind). No backend. | Runtime is lookups + rendering; static = zero ops, offline-safe demo, Vercel-native. Swap to route handlers over DuckDB only if a per-race JSON exceeds ~5MB. | Albert + master |
| D-03 | Contracts as Zod + TS in `contracts/`, mirrored to JSON Schema for Python. Pipeline output must validate. | Catches drift between data and UI workstreams mechanically. | Albert + master |
| D-04 | 10-issue taxonomy frozen (`contracts/src/issues.ts`). | Shared join key across votes, bills, stances, donors, ads. Same list for every candidate → side-by-side comparable. | master |
| D-05 | Chains: walk backward until termination (individual / non-committee org / cycle), 8-hop safety cap, visited-set, prune inbound edges <1% of receiver's receipts into an `agg:other@<id>` node. | Party/JFC hops fan out into tens of thousands of donors; pruning keeps dollars conserved and graphs legible. | Albert + master |
| D-06 | Traceability V0 = disclosed share of outside dollars (weighted by spend in race), labeled "preliminary". | Full definition needs conduit and 990 handling; ship a defensible proxy first. | master |
| D-07 | Conduits handled now, not deferred: drop `MEMO_CD='X'` rows, attribute `15E` to the individual, exclude conduit committee from the transfer graph, expose `via_conduit_total`. | Otherwise ActBlue/WinRed are the top donor everywhere. | master |
| D-08 | Filtered Parquet committed under `data/fec/<race>/`; raw bulk gitignored under `data/raw/`; `make ingest` reproduces. | Raw indiv24 is ~2GB zipped; teammates need a working repo without it. | Albert + master |
| D-09 | No LLM anywhere. Dossier stance text hand-written from the record, `needs_review: true`. | Cost, and verifiability on stage. | Albert |
| D-10 | Cycle-specific 2024 totals for candidate committees (not career). | Comparability between incumbent and challenger. | Albert |
| D-11 | Evidence render = link-out to the government record. Drawer is a polish item. | Fastest to correct; every link is itself the receipt. | Albert |
| D-12 | Demo stories are computed (`stories.json`: top by amount, top by dark share, popups) and a human picks. | Don't guess the narrative; let the numbers surface it. | Albert |
| D-13 | Web reads `data/out` via `fs` in server components; `generateStaticParams` on every dynamic route. | Keeps `output: "export"` a one-line switch; pages fail at build (not on stage) if a contract is violated. | master |
| D-14 | System fonts instead of `next/font/google`. | Build must not need network. | master |
| D-15 | Children merge to `main` via master (no human review overnight); PRs kept as the record. | Albert's call. | Albert |
| D-16 | Frontend A shared additions are new files under `web/src/components/ui/` (`stacked-bar.tsx`, `table.tsx`, `party-tag.tsx`), imported by path; `ui/index.tsx` untouched. Campaign-vs-outside bars use a neutral near-black/grey palette (`MONEY_COLORS`), never the visibility colors. | Avoids merge conflicts with Frontend B and keeps green/amber/red reserved for disclosure visibility. | Frontend A |
| D-17 | Ledger spender table is the only client island (`"use client"`, sort by name/total/traceability). Per-spender visibility dot is derived: `dead_end_dark` flag → dark; else traceability ≥0.5 disclosed, ≥0.2 inferable, else dark; `null` → hollow dot. | Contract has no per-spender `visibility` on `OutsideSpender`; a proxy is better than no dot. Contract wish: add optional `visibility` to `OutsideSpenderSchema` so the pipeline can set it directly. | Frontend A |
| D-18 | Entity page shows the chain CTA only when `has_chain && chains/<id>.json exists`; counterparties link to entity pages only when the counterparty has an entity file in this race. "Other" campaign receipts = receipts − individuals − committees, floored at 0. | Never emit a dead link; never render a negative residual from inconsistent totals. | Frontend A |
| D-19 | Ads: bundle fetched from `storage.googleapis.com/political-csv/…` (the transparencyreport URL now serves a 2.5KB stub README pointing there). Queried straight from CSV with DuckDB each run; raw stays gitignored. | The documented URL is dead for data; one 2.8GB CSV scan is ~5s, so no intermediate Parquet is worth committing. | Ads child |
| D-20 | Ads: race relevance = candidate committees keep every US ad; all other advertisers (seed/ledger committees, names containing "Casey"/"McCormick") must have `Geo_Targeting_Included` naming Pennsylvania (state, PA DMA, or "City, PA"). Untargeted national ads from multi-race PACs (NRSC, DSCC, SLF…) are excluded. | Bundle `Regions` is country-level; the ad-level geo column is the only PA signal. Including untargeted national creatives would flood the gallery with ads about other races. | Ads child |
| D-21 | Ads: `paid_for_by` is null for every ad; bundle-level `source_url` lives in `notes[0]` rather than a new contract field. Seed list is hand-verified against cm24 (IDs in `ads_match.py`); seed entries win over ledger names on conflict; "Pennsylvania Values" dropped (no FEC committee, no Google advertiser). | Google's declared-name table covers only CA/NZ advertisers. `AdGallerySchema` has no top-level `source_url` and every ad already carries its own record URL; avoided touching the shared contract. Mock ledger carries a wrong NRSC id. | Ads child |
| D-22 | Ads: creatives cached only for VIDEO ads in the top 50 by spend upper bound, as the YouTube poster JPEG resolved via the transparency site's lookup RPC → preview JS → `video_id`; TEXT/IMAGE creatives keep `cached_creative_path: null`. 26 of 50 cached, ~300KB. | The site is a JS app rendering creatives in iframes; the poster frame is the only static asset reachable without a headless browser. Best-effort: any failure leaves null. | Ads child |
| D-23 | Dossier stage is curated-literals + fetch/verify: `dossier_curated.py` holds roll numbers, bill ids, issue tags, positions and archived quotes; `dossier.py` fetches senate.gov XML / congress.gov API / Wayback and raises on any miss (no fallback data). Casey `evidence_basis: "record"` (contract has no `votes_and_bills`), McCormick `"statements"`. McCormick `healthcare` is tagged from the veterans section (only health-care text on the archived issues page), `labor_trade` from the China plan. `roll_call_vote` items tagged to several issues are repeated under each. | No LLM; every sentence hand-written; a human can edit one file in the morning and rerun. | dossier child |
| D-24 | Chain view is a hand-rolled server-rendered SVG layered layout (columns = `depth`, ribbons = cubic Béziers, width ∝ amount), no `d3-sankey`. UI prunes to 24 nodes by amount with an "n more" note; the edge table below always shows every edge. | ≤ 60 nodes never needs a real Sankey solver; zero client JS and no layout flash; receipts stay complete even when the picture is pruned. | frontend-b |
| D-25 | Ad gallery filter/sort is the only `"use client"` island in Frontend B; `AdCard` stays a plain component fed serialisable `Ad` records. Cached creatives use a plain `<img>` with a local eslint-disable. | Cards are static; only the toggle/sort needs state. `next/image` would need a loader for arbitrary cached paths. | frontend-b |
| D-26 | Dossier evidence is ordered by the contract's `EVIDENCE_KINDS` order (roll call > sponsored > cosponsored > stated > statement), then date desc. Yea/Nay rendered in neutral ink, never green/red. All 10 `ISSUES` always render, missing ones as "No record loaded". | Revealed preference before stated preference; a vote colour would read as an endorsement. Fixed issue grid makes the two dossiers comparable at a glance. | frontend-b |
| D-27 | FEC ingest: Schedule E comes from the FEC processed bulk CSV (`fec.gov/files/bulk-downloads/2024/independent_expenditure_2024.csv`) loaded with DuckDB, not paginated from the OpenFEC API. Dedupe key `(committee, candidate, exp_date or dissem_dt for notices, round(amount,2), upper(payee))`, non-notice > notice, latest amendment wins, non-positive rows dropped. 1,698 raw -> 1,609 rows, $237.5M. | No `FEC_API_KEY` was provisioned; `DEMO_KEY` is 40 calls/hour and the two candidates need ~20 pages each with retries. The processed file carries the same columns (amount, dates, payee, purpose, file/image numbers) and the filing-image URL is derivable from `image_num`. | FEC child |
| D-28 | FEC ingest: candidate and committee financial summaries come from `weball24` / `webk24` with hand-transcribed column lists (the `_header_file.csv` for those two returns `NoSuchKey`). Ledger `campaign.*` and entity `totals.*` prefer these FEC summary totals over itemized sums (itemized sums are the fallback for committees without a summary row). | The FEC summary files are the same numbers `fec.gov/data/candidate/...` displays, so the visible number matches its `source_url`; itemized Sched A/B under-count unitemized receipts and operating expenditures. | FEC child |
| D-29 | FEC ingest: neighborhood expands through both the receiver's Sched A (`oth`, receipt TTs + 15Z/17R/17Z/19) and the sender's Sched B (`oth`+`pas2`, disbursement TTs minus 24I/24T conduit pass-through), 8 hops, conduits never expanded. Closure reached 6,844 committees; capped to the 2,000 with the most dollars sent into the neighborhood. | Sched-A-only expansion misses transfers the receiver reported only as a memo; 24I/24T are earmark pass-throughs, not money edges. 2,000 keeps `data/out` (~46MB of entity JSON) and Parquet (17MB) committable. | FEC child |
| D-30 | FEC ingest: individual contributions are aggregated per `(committee, name, zip5, employer, occupation, TT, conduit)`; seed committees (2 campaigns + 77 outside spenders) keep every donor identity, other neighborhood committees keep ORG donors + their 100 largest individuals; conduits' own Sched A is omitted. 15E rows are attributed to the individual with `CONDUIT_ID` = OTHER_ID. | 58M `indiv` rows / ~9k committees; the party committees alone have millions of donor rows. The ledger only renders top-25 counterparties per entity, so the tail is never visible; conduit Sched A would double-count the 15E earmarks at the receiving committee. | FEC child |
| D-31 | FEC ingest: transfers dedupe receiver Sched A vs. sender Sched B on `(from, to, date)` (per-day sums per side); receiver row kept, `amt_other_side` + `mismatch` (>1%) columns retained in Parquet for the chains stage. 358k edges, 1,266 mismatches. | Spec step 7. The mismatch flag is data for `chains.py` to surface; ledger leaves `flags: []`. | FEC child |
| D-32 | Chains: graph edges are committee transfers (receiver rows from `transfers.parquet`, 22Z refunds dropped) plus Sched A rows from `individual_contributions.parquet` with ENTITY_TP IND/CAN (individual, disclosed) or ORG (organization, dark) and receipt TTs 1x/3x. Sched A rows typed PAC/COM/CCM/PTY are not edges. A node's "total receipts" is the sum of these loaded itemized edges, not the `webk24` summary total. | PAC/COM rows are the same dollars as the transfers table and would double count. Summary totals include unitemized receipts, loans and offsets that have no counterparty, so conservation (sum of inbound edges == node total) can only hold against the itemized base; `method` states this. | Chains child |
| D-33 | Chains: per node, edges below 1% of receipts *or* beyond the 40 largest roll into `agg:other@<node>` (optional `contributor_count` added to `ChainNodeSchema`); a chain stops expanding once it holds 600 nodes (remaining committees become `depth_cap` termini carrying their edge amount). Cycle nodes are `cycle:<id>@<parent>` and reuse the mix of the committee they refer to. | The DSCC alone produced 15,750 nodes under the pure 1% rule (the party committees have thousands of >1%-free material edges through joint fundraisers); 600 nodes keeps every chain file under ~1MB and the graph view usable. Dollars still conserve at every expanded node. | Chains child |
| D-34 | Chains: a committee whose counterparty is an FEC-registered committee outside the loaded 2,000-committee neighborhood (or loaded with no receipts) terminates as `depth_cap` and counts as disclosed. `popup` uses first observed activity (first receipt or IE in the bulk data, ignoring January 2023) as the registration proxy, flagging first activity after 2024-10-17 or a first IE within 60 days of the first receipt. `inferable` is never emitted (no 990 lookup). | `cm24` carries no registration date; first activity is the only date in the loaded data. Disclosed-but-not-loaded is not dark (master note). | Chains child |
| D-35 | Chains flags: `shell_cluster` requires 2..10 committees sharing normalized street (suite/unit stripped) + treasurer; larger clusters (compliance firms at 228 S Washington St, 824 S Milledge Ave, ...) are logged and skipped. `transfer_mismatch` is one flag per receiving committee listing its top mismatched senders. Form 5 filers (`CMTE_TP` I) have no receipts: `has_chain: false`, and their IE dollars count as dark in `traceability`. Traceability weights every Sched E dollar in the race by its spender's `disclosed_share`. | 7 addresses host 11–38 committees each, which is a treasurer's client list, not a shell cluster. Form 5 filers are 501(c) orgs spending directly with no donor disclosure. | Chains child |
| D-36 | Schedule E comes from the OpenFEC API (`/schedules/schedule_e/?is_notice=false&most_recent=true`, keyset pagination), not the bulk IE file. Notices (24/48-hour, Form 5/F3X Sched E "N") and superseded amendments are excluded at the source; non-positive rows dropped. The bulk file is no longer downloaded. | The critic found the local notice/periodic dedupe (date + rounded amount + payee) mismatched fec.gov's candidate totals for Keystone Renewal PAC and others; notice dissemination dates differ from periodic expenditure dates, and some spenders (WINSENATE) appeared only as notices in the filtered bulk subset, so "drop notices" was unsafe there. The API's `most_recent` periodic set reproduces fec.gov's own by-candidate totals (WINSENATE $60.2M oppose McCormick, SLF $52.8M). 98 spenders, $235.7M. | Master |
| D-37 | `popup` fires only when first observed activity (first receipt or IE) is after the pre-general cutoff (2024-10-17); the "first IE within 60 days of first receipt" path is removed. `shell_cluster` excludes candidate committees, JFCs, and committees sharing a `CONNECTED_ORG_NM` with another member. | The 60-day path flagged ordinary committees and produced copy like "within -3 days"; a sponsor's PAC family sharing an agent is ordinary, not structural. | Master |
| D-38 | Sched A `ENTITY_TP='ORG'` contributors are classified from their name (`orgs.py`): union / business → disclosed terminus `organization` (the named entity gives from its own treasury and is the source); LLC / trust / advocacy-style nonprofit / unclassifiable → dark. Advocacy words beat corporate suffixes ("LEAGUE OF CONSERVATION VOTERS, INC." is a 501(c)(4)). `ChainNode.organization_class` carries the class; the ledger's entity inflows use the same rule. No IRS lookup in V0; `inferable` still unused. | Critic C-01: calling Coinbase or a carpenters' union "dark" misuses the PRD's definition (dark = hidden donor layer). A name classifier is imperfect (bare "COINBASE" is `unknown` → dark, conservative), but it is transparent and every node says how it was classed. | Master |
| D-39 | Design system (research only, `docs/DESIGN.md`): keep `VISIBILITY_COLORS` semantics and fill values; add darker `*-text` siblings (`#0E7A57` / `#9A5B00` / `#B42323`) for text on white, darken the inferable fill to `#B8760F`, give campaign vs outside two non-party hues (`#1E293B` / `#6D4BD8`), Inter + IBM Plex Mono (OFL) via `next/font`, tabular figures everywhere, flags as short `⚑` chips, chain diagram top-8-per-hop by default. Tokens live as CSS variables + Tailwind 4 `@theme inline`; no component library added. | Current green/amber/red fail WCAG AA as text (3.4 / 2.2 / 3.9 : 1) and vanish on a projector; the two money kinds had no colour identity while visibility had three. Everything fits Next 15 + Tailwind 4 with zero new dependencies; shadcn/ui is optional step 15. Not applied — another session owns `web/`. | Design child |
| D-40 | Chain page (C-10): the Sankey is a client component fed a compact wire form of the chain (node tuples, index-based edges) and renders a pruned view — root, its direct sources, and every node with `amount_in` ≥ 2% of root receipts (max 40); the rest fold into the parent's `agg:other` node (synthesised client-side when the pipeline emitted none). Clicking a node expands its sources without navigation. The edge table stays server-rendered for the top 100 edges by amount; the tail is a client "show all" toggle. Dollars at visible nodes and the summary shares are unchanged. | WINSENATE HTML 2.1 MB → 540 KB; the pipeline's dollar conservation is preserved because folding only regroups existing edges. | critic-p1 session |
| D-41 | Traceability has a fourth bucket, `unwalked` (C-06): dollars terminating at `depth_cap` (FEC committee outside the loaded neighborhood, hop cap or node cap) are neither disclosed nor dark. Contract-additive: `ChainSchema.summary.unwalked_share`, `TraceabilitySchema.unwalked`, `OutsideSpenderSchema.visibility_shares` (all optional). Score stays disclosed / total, so it drops (race 0.729 → 0.721, Casey 0.751 → 0.749, McCormick 0.703 → 0.689, WINSENATE 0.66 → 0.64). Rendered neutral grey `#73726c` (`UNWALKED_COLOR`), labelled "Not walked (FEC committee, receipts outside this race's neighborhood)". Supersedes the "counts as disclosed" clause of D-34. | Counting an unread committee as disclosed overstated traceability; counting it as dark would invent a hidden layer. Grey is honest about what the walk did not read. | critic-p1 session |
| D-42 | `transfer_mismatch` is not emitted between a candidate's principal committee (`CMTE_DSGN='P'`, has `CAND_ID`) and a JFC (`CMTE_DSGN='J'`) it takes part in (C-09). Participation is inferred, any of: JFC carries the same `CAND_ID`; the principal's `CONNECTED_ORG_NM` normalizes to the JFC name; the candidate's surname (from `cn` `CAND_NAME`) appears as a word in the JFC name. Casey ↔ Casey Keystone Victory Fund drops (17 → 16 flagged entities); all other mismatches survive. `write_back` now rewrites every entity that carried flags/has_chain so a lost flag actually disappears from `entities/*.json`. | JFC participants are not in the committee master; the JFC's Schedule B and the principal's Schedule A routinely disagree on timing/allocation within the same campaign, which is bookkeeping, not a story. | critic-p1 session |
| D-43 | Evidence URLs are pair-filtered (C-08): chain edges and entity inflows link the *receiver's* fec.gov receipts search narrowed to the sender — committees via `&contributor_name=<sender CMTE_ID>`, individuals/organizations via `&contributor_name=<reported NAME>`; aggregate ("other") nodes stay committee-wide. The assignment suggested `contributor_committee_id`; fec.gov's browse UI ignores that (and `contributor_id`) and only honours its "Source name or ID" box, which serializes as `contributor_name` and accepts a committee ID. Browser-verified: WINSENATE←SMP (38 rows) and BMO PAC←"CASPER, DAVID R" (15 rows). | The user must land on the exact rows behind the number, not the whole committee's ledger. | critic-p1 session |
| D-44 | Removed four never-read contract fields (C-13) — the explicit exception to "never remove fields": `CandidateLedgerSchema.top_contributors` (+ `ContributorSchema`, `ledger.top_contributors()`), `AdSchema.paid_for_by`, `AdSchema.spend_range.currency`, `AdSchema.issue_ids`. Dropped from Zod, regenerated JSON Schema, `ads.py`/`ledger.py` emitters, the mock generator, the one UI reader (`ad-card.tsx` disclaimer line) and stripped in place from committed `ads.json` (ads were not re-run — the 2.8 GB Google bundle is not needed for a field deletion). `declared_name` stays in the ads bundle as a matching hint. | No consumer existed; `top_contributors` was mislabelled (JFC transfers), `paid_for_by` always null, `issue_ids` always `[]`, `currency` a literal. | critic-p1 session |
| D-45 | Stories render additively. `StorySchema.verified_by_url` (optional URL) is the only contract change; `verified: true` without it still shows the unverified chip. Ledger gets a "Start here" strip of the top 5 (`StoryCard`), `/races/[raceId]/stories` lists all 17. Generator language tightened in `chains_stories.py`: "dark" narratives now say "an organization with no FEC donor-disclosure filing on record; the chain stops there" and "stop at organizations whose own funding is not on file with the FEC" — the earlier "money from nowhere"-style phrasing implied concealment the record does not show. Regenerated `stories.json`; every narrative re-read for causal verbs (none). | Stories are templated from chain data and unverified; the UI must say so on every card, and the prose must describe what is on file, not what is hidden. | v1 session |
| D-46 | Ad→chain links are hand-verified, not name-matched. `pipeline/campaign_commons/data/ad_verifications.json` is hand-maintained (5 records: WINSENATE `C00865444`, SLF `C00571703`, AFP Action `C00687103`, Keystone Renewal `C00849489`, DSCC `C00042366`), `ads_verify.py` validates it (human verifier, ≥2 evidence URLs, ID/entity agreement), `make ads` merges it as additive `verification: {status, evidence_urls, verified_at}` on every ad (unverified ads keep their `auto` name match). `ad-card.tsx` shows "Verified paid-for-by → chain" only on `status == "verified"` (replaces the C-11 chain-file gate); the chain page shows a "You may have seen this ad" strip for its verified ads; the gallery has a Verified filter. Caveat recorded in each `note`: Google's rendered ad page does not expose a literal "Paid for by" line; what was read is the advertiser's legal name and (for 4 of 5) its FEC ID, then matched to the fec.gov committee record and its PA-Sen Schedule E rows. AFP Action's advertiser page showed an EIN, not an FEC ID; the committee was matched by name. | A chain link on an ad should mean a person confirmed the payer, not that a file happened to exist. | v1 session |
| D-47 | Forward donor view reuses the chain graph. `donors.py` inverts `Graph.inbound` and walks donor → committees given to → outside spenders in this race (via transfers) → IE targets, for the 50 largest individual/organization sources by summed `amount_in` across chains (`total_in_chains`, a ranking weight that counts a gift once per chain it appears in — documented in `method`). Money edges (`kind: "money"`) and IE targeting edges (`kind: "targeting"`, `support_oppose`) are separate; a candidate is only ever reached by a targeting edge. No allocation past the first hop: when any spender is reached through an intermediary the file carries `allocation_note` = "share of donor's dollars reaching this spender is not determinable; showing the committees in between" and the page renders it verbatim. 200-node cap (`truncated`). Contract `DonorViewSchema` (`donors/*.json`), covered by both validators. Chain nodes with a donor file link to `/races/[raceId]/donors/[key]`. Note: source kind (`ind:` / `org:`) is whatever FEC's `ENTITY_TP` says, so a few `ind-*` files are trade associations. | Once pooled, a donor's dollars are fungible; showing the path without inventing a share is the honest reading. Reusing the edge tables keeps forward and backward walks on identical data. | v1 session |
| D-48 | Schedule E (C-29): after `most_recent=true`, the same transaction can still appear once per periodic filing that re-reports it. `fec_ie.dedupe_reported` keeps one row per `(committee, candidate, support/oppose, tran_id, payee, amount, dissemination date)`, highest `file_num` wins; rows without `tran_id` are never merged and `tran_id` alone is never a key (filers reuse it). PA 2024: 2,277 → 2,235 rows, $235.67M → $233.40M, matching OpenFEC `by_candidate` per spender. |
| D-49 | Schedule A non-individual rows (C-30/C-31): a row whose normalized name is exactly one committee in the master (`orgs.committee_name_index`), or that the filer typed PAC/COM/PTY/CCM, is a committee — never an organization. If the sender's own filing is already in `transfers` the row is the receiver-side copy and is dropped (`covered`); otherwise it is emitted as a disclosed committee inflow / graph edge. Generic markers (`PAC`, `FUND`, `ACTION`, `COMMITTEE`, `CHAMBER`, `AMERICA(NS)`, `VICTORY`, `527`) alone no longer make a name a nonprofit; corporate suffixes win over them. |
| D-50 | Donor view (C-32): `allocation_note` is set whenever any amount past the donor's own gifts is shown (a transfer or an IE), not only behind an intermediary — those amounts are the reported totals between two parties, never the donor's share. |
| D-51 | Aggregated flow rows (C-36): `Transfer` gains optional `count` and `first_date`; `date` stays the latest transaction and `transaction_type` is documented as the modal code. Web renders "first – last" and "n transactions" when a row rolls up more than one. |
| D-52 | Ad copy (C-33/C-34/C-35): Google publishes no US paid-for-by line, so verified ads say "sponsor verified by hand" (advertiser legal name ↔ FEC record); Sponsor links render only when the entity page exists; the entity header's IE figure is labelled "all races, 2024 cycle" and links the committee's fec.gov Schedule E. |
| D-53 | Race pages share a section nav (`RaceNav`: Ledger · Ads · Stories · one dossier per candidate, with counts) rendered under the header on ledger, ads and stories pages. Ads had been reachable only via a small card action and direct URL. |
| D-54 | Docs structure: `docs/README.md` index, `FAQ.md` (glossary + answered questions), `QUESTIONS.md` (`Q-nn`, open; deleted when a `D-nn` lands). Lifecycle: asked → FAQ or QUESTIONS → DECISIONS → FAQ links the D-nn. |
| D-55 | Block 2 contracts, additive only. `Basis {basis: filed|verified|inferred|adjacent, rule, source_urls, checked_by, checked_at}` is attached to every relationship or number not read straight off a record; the UI renders `rule` wherever it shows the relationship, and graph edge style is derived from `basis` (solid / solid+check / dashed / dotted). New files: `vendors.json` + `vendors/<id>.json` (Schedule E payees normalised; `normalization` is itself a Basis), `issues.json` (two layers, never summed: `by_ad_issue` = what tagged ads/IE notices were about; `by_spender_focus` = what the spender says it is for), `search.json` (static client index). Extended: IE rows get `vendor_id`/`medium`/`issues {issue_ids, basis}`; `Entity` gets `vendors[]`, `issue_focus`; `Ad` gets `sponsor_visibility_shares` (dark share shown as a number), `issues {issue_ids, basis}`, `vendor_links[]` (each with a Basis); `vendors.json` carries one `medium_basis` for how every `medium` was classified; `ChainNode.kind` gains `vendor|ad|candidate` with `side: "out"`, `ChainEdge.kind` gains `placement|targeting`. | Nothing filed joins an FEC payment to an ad-library creative or says what a party PAC is "for"; the product must show the receipts *and* say which claims are ours. | master (Block 2) |
| D-56 | Hand-maintained inputs live in `data/hand/<race_id>/{issue_focus,ad_issues,ie_issues,vendor_aliases,vendor_ad_links}.json`, each `{race_id, method, rows[]}`, every row with `source_url(s)` and `tagged_by`. Validated by both validators (`make validate` runs `data/out` then `data/hand`; JSON Schema `hand_*.schema.json`), merged in by the pipeline (`make vendors`, `make issues`, `make ads`), never edited in `data/out`. Teammates edit these files directly. | The V1 gaps (funder focus, ad topics, verified vendor→ad pairs) are human research; keeping it in typed, sourced, validated files keeps it reviewable and out of the code. | master (Block 2) |
| D-57 | Payee → vendor normalisation (`campaign_commons.vendors`): uppercase → `L.L.C.`→`LLC` → punctuation to spaces → collapse whitespace → strip *trailing* legal suffixes (INC, INCORPORATED, LLC, LP, LLP, PLLC, LTD, LIMITED, CO, CORP, CORPORATION, COMPANY; GROUP is not a suffix; never strip the last token) → token-sort key. Residual near-duplicates fold with stdlib `difflib.SequenceMatcher(autojunk=False)` ratio ≥ 0.92 **and identical numeric-token sets**; every fold is printed (`merge 'A' -> 'B' (ratio r)`). `data/hand/<race>/vendor_aliases.json` is applied first and wins (basis `verified`, `checked_by/at` from the row; `source_urls` = fec.gov payee view, then the row's own source). Rule text on every vendor: `case/punctuation/suffix normalisation + token-set ≥0.92 (numeric tokens must match); aliases file`. No `rapidfuzz`: difflib found the only typo pair (GENRIS/GENRRIS RUMALDO) and every other candidate pair was a false friend. | PA 2024 has `UNITE HERE LOCAL 23 / 25 / 26 / 34 / 74 / 878 …` at ratio 0.93–0.97 — different unions, so a plain 0.92 threshold merges what must stay apart; requiring the digits to agree keeps the threshold and the false-friend guard in one deterministic rule a reviewer can read off the log. | vendors child |
| D-58 | `medium` is classified from the filed `purpose` by one ordered keyword table, first match wins, keywords anchored at word starts (so `GOTV` is not `TV`, `LISTED` is not `LIST`): production (produc, creative, design, videograph, photograph, filming) › digital (digital, online, internet, web, social, streaming, email, youtube, facebook, google, meta, gif) › radio › tv (tv, television, broadcast, cable, media buy, media placement) › mail (mail, postage, postcard, print) › phones (phone, text, sms, robocall, call, dial, teleconferenc) › consulting (poll, research, consult, strateg, list, targeting, voter file) › other. A hand alias row's `medium_override` replaces the classification for that vendor (Meta/Google/Hulu → digital, USPS → mail, iHeart → radio). The table is the module docstring, `vendors.json.notes[0]` and `vendors.json.medium_basis.rule` (basis `inferred`, source = FEC Schedule E file description); the UI renders that rule once per section wherever a medium appears, under the fixed caveat "Medium is classified from the purpose the spender filed; the FEC does not record which buy placed which ad." Generic `MEDIA BUY`/`MEDIA PLACEMENT` with no digital/radio/mail word counts as tv. Result: 93.9% of $233.4M classified as something other than `other`. | The purpose string is the only thing filed about what a buy was; a fixed, printed table is honest about how coarse that is and lets anyone re-derive a row's medium by hand. | vendors child |
| D-59 | `vendor_id` = `V-` + slug of the canonical normalised name (`V-waterfront-strategies`), or the id written in the alias row; canonical name = the raw payee string carrying the most dollars in the cluster (hand rows supply their own display name). Ids are deterministic from the data + alias file, so `make vendors` is idempotent (stale `vendors/*.json` are deleted). Note the `VendorSummary.vendor_id` comment in `schemas.ts` says `vendor:<slug>`; the assignment said `V-` and that is what is emitted — the chain child should derive node ids from `vendor_id` rather than assume either prefix. | A stable, readable id that survives re-runs and does not collide with FEC `C…`/`S…` ids; slugs make the vendor URLs legible. | vendors child |
| D-60 | Contract change (optional, additive): `HandVendorAliasRow.tagged_at?: string`. A hand alias row becomes a `verified` Basis, and `verified` requires non-null `checked_at`; without a date on the row the pipeline would have to invent one. JSON Schema regenerated (`hand_vendor_aliases.schema.json`). Vendor `source_url` is always the fec.gov Schedule E browse filtered to the payee strings (`?cycle=2024&data_type=processed&payee_name=…`, up to 10 strings, `&q_spender=<id>` for the per-entity rows) — browser-checked; `is_notice=false` returns nothing on that view and is omitted. | Every number needs a government-record link; the alias row's own source (e.g. a company rename announcement) is evidence for the fold, not for the dollars. | vendors child |
| D-61 | Chain graph is a two-sided, interactive picture. Funding walks left of the spender (`side: "in"`, hop columns), spending right of it (`side: "out"`: paid vendors → ads → targeted candidates). Money edges are dollar-proportional ribbons; `placement` and `targeting` edges are thin spines whose dash is the Basis (solid filed/verified · dashed inferred · dotted adjacent) with an arrowhead, so a targeting edge can never be read as dollars reaching the candidate. First click on a node opens a per-kind panel (`node-panel.tsx`): amounts, the Basis sentence and source links for every incident edge, ad thumbnail, vendor medium, candidate for/against split, then "open full page" / "record ↗". Controls per node: show/fold its funding sources, expand/collapse what hangs off it, hide it (with a restore count in the caption). Small sources (<2% of receipts, beyond the 40 largest) stay folded into dashed "other" nodes until opened. Wire format (`view.ts`) is a tuple array so the largest chain page stays under 800KB. | The user asked for a Palantir-Vertex-style graph: basic info on first click, full page on demand, hide/expand. Encoding evidence in the edge itself keeps the "assumption" visible even when the panel is closed. | master (Block 2) |
| D-62 | Search index is served by a `force-static` route handler (`web/src/app/search.json/route.ts`) that reads `data/out/search.json` through `getSearchIndex()` at build time — not a copy into `public/`. Matcher (`components/search/match.ts`): case-insensitive substring on label/aliases OR every query token is a prefix of a label/alias token; rank exact > prefix > substring > token-prefix, then `weight` desc, then label; top 8 grouped by kind in rank order. `SearchItem.weight` is whole dollars (race: campaign + outside; committee: receipts + IEs or the largest available total; organization/donor: itemized total given; vendor: total paid) and is a tiebreak only, never rendered. Aliases capped at 8; FEC id is always an alias. | One build-time source of truth (no copy step to forget); a few hundred lines of plain TS beats a fuzzy-search dependency for ~2,000 rows; dollars are the only ranking signal every kind has. | child 4 (Block 2) |
| D-63 | Ad ↔ vendor window rule (`campaign_commons.ads_enrich`): a sponsor's vendor is linked to an ad when any of the sponsor's Schedule E rows paid to that vendor is dated in `[first_shown − 7 days, last_shown]`, both ends inclusive; the link carries `medium`, the window, `amount_in_window`/`buys_in_window` (sums of exactly those IE rows) and `basis: adjacent` whose `rule` names the window and says the FEC does not record which buy placed which ad. Ads missing either date get no links. | Vendor invoices precede flights; a week is the shortest lead that still catches a buy reported the week before the creative first ran. Anything longer starts linking unrelated flights; the rule text is on every card so the reader can disagree. | media-wall child |
| D-64 | Single-digital-vendor inference: when exactly one vendor with `medium == "digital"` has buys inside an ad's window, that link is `inferred` ("only digital vendor {sponsor} paid during this ad's run window — inferred, not filed"); two or more digital vendors, or none, leave every link `adjacent`. `verified` needs a `data/hand/<race>/vendor_ad_links.json` row naming (ad_id, vendor_id) with sources and a tagger, and overrides the computed basis; verified links sort first. Nothing is ever promoted to `filed`. | Google ads are digital placements, so one digital payee in the window is the strongest inference the filed record supports — and it is still an inference. | media-wall child |
| D-65 | `ads.json` has one writer after `campaign_commons.ads`: `campaign_commons.ads_enrich` (`make ads-enrich`, in `all` after `ads`/`vendors`) patches `sponsor_visibility_shares` (from `chains/<sponsor>.json.summary`), `issues` (from hand `ad_issues.json`, basis `verified` / "Tagged by a person from the creative"), `vendor_links[]`, the `Enrichment (…)` notes, and the reverse `vendors/<id>.json.ads[]`. Every field is recomputed from inputs and prior enrichment notes are replaced, so re-running is byte-identical; it never downloads the Google bundle. Other stages read `ad_issues.json` but do not write ads. | Two writers of one file is how idempotence dies; enrichment must be re-runnable after `vendors`/`chains` change without re-fetching 2.8 GB. | media-wall child |
| D-66 | Two issue layers, never summed or compared. `by_ad_issue` attributes dollars to the *record* (a tagged ad or a tagged 24/48-hour notice is that spending). `by_spender_focus` groups *spenders* by their self-description and sums their outside totals; the copy is always "spenders who describe themselves as focused on X account for $Y", never "$Y was spent on X". The two cards sit side by side with an explicit non-summability note; `Entity.issue_focus.basis.rule` says the same on every entity page. | A party PAC's $52M is not "about" anything; putting it next to ad topics would launder self-description into spending analysis. | issue-focus (Block 2) |
| D-67 | Google ad spend is a range per creative; `by_ad_issue` sums `spend_min`, `spend_max` and the midpoint over tagged ads and the UI shows the midpoint labelled "≈". Midpoints are never added to FEC IE dollars — the two are shown as separate bars in the same row and the basis `rule` says so. | The range is the only figure Google publishes; a midpoint is the least-wrong single number and the bounds stay in the file. | issue-focus (Block 2) |
| D-68 | A record with several issue tags counts its full amount under every tag (ads and IEs alike); issue rows in `by_ad_issue` therefore overlap and do not sum to a total. Same in `by_spender_focus` with `primary_only: false`; `primary_only: true` puts each spender under its first tag only and partitions the tagged dollars exactly (asserted in tests). | Splitting one ad's dollars between "healthcare" and "guns" would invent a proportion nobody filed. | issue-focus (Block 2) |
| D-69 | `general_partisan` = an organisation whose own description names an electoral or ideological goal (win seats, build a majority, "elect progressive candidates", "promote conservative policies") and no policy area from the frozen taxonomy: party committees, leadership super PACs and their c4 arms, coordination hubs. When a description names no frozen issue but is not partisan-general, the closest id is used and the row's `description` says it is a loose fit; the id is never left off an issue kind. Where no self-description could be found the row is omitted and counted in `coverage`, never written from the spender's name. | The headline is the split itself: 59% of PA outside dollars come from committees that name no issue. | issue-focus (Block 2) |
| D-70 | Project renamed **Citizen Gotham → Campaign Commons**: npm scope `@campaign-commons/contracts`, Python package `pipeline/campaign_commons` (was `gotham`), pyproject `campaign-commons-pipeline`, site title. Repo URL unchanged until the GitHub rename (old URLs redirect). Historical docs (LOG entries, CRITIQUE rounds, design mockups) keep the old name. | "Campaign" data brought into the commons — the name says what the product does; `gotham` was an internal codename. Renamed the Python package too so `method`/`rule` strings in generated JSON (`campaign_commons.vendors`) point at real files. | master (Block 2) |
| D-71 | Adjacent vendor ↔ ad links are offered only for `digital`, `production` and `other` media (`ADJACENT_MEDIA` in `campaign_commons.ads_enrich`); `verified` links may be any medium, `inferred` stays the single-digital-vendor rule (D-64). The adjacent `rule` text says that same-window TV/radio/mail/phone/field/consulting buys are not listed. Refines D-63. | A same-window mail or TV buy cannot have placed a Google creative; listing it as the ad's vendor read as a claim, not a co-occurrence (critic C-51: an NRA ad card listed a mail, a phone and an "other" vendor). `other` is kept because it means "purpose string unclassified", not "not digital". Adjacent links 975 → 497. | master (critic r3) |
| D-72 | Every ad has its own page, `/races/<race>/ads/<ad_id>` (500 in PA). It shows the creative large, the platform ranges (midpoint labelled as such), sponsor match + dark share, issue tags, the full vendor-link list with rule and sources, and a chain graph *re-rooted around the ad*: the sponsor's whole funding side, and on the spending side only the vendors this ad links to, the ad, and its targeted candidates (`components/chain/ad-view.ts`, built at render time from `Ad.vendor_links` + `chains/<sponsor>.json`, same rules as `chains_out._ad_parent_edge`). Ad cards in the wall keep one line per vendor (name + basis) and link to the page; `AdThumb`, `SeenAdsStrip`, `VendorAds` and chain ad-node `href`s all point there instead of `/ads#<id>`. | The wall card was carrying every rule and source for every ad (3 MB page) and the spender chain drew all vendors and top-10 ads at once; the reader asked for one ad at a time with the chain walked backward from it. Building the focus graph in the web layer avoids emitting 500 ad nodes per chain, but the placement rules now live in two places (Python for the spender chain, TS for the ad page) — flagged for the next critic. | master (ad pages) |
| D-74 | **Adjacent vendor→ad links dropped.** `Basis.basis` and `AdVendorLink.basis` are `filed | verified | inferred` only; `adjacent` is gone from the contracts, the pipeline (`ADJACENT_MEDIA` → `PLACEABLE_MEDIA`, used only for the new context list) and every legend/filter. A vendor→ad edge exists only when a person verified it from a source naming both (`verified`, solid) or it was the only digital vendor the sponsor paid in the window (`inferred`, dashed; D-64). Date overlap alone is kept as **context, not a relationship**: `Ad.same_window_buys[]` (vendor, medium, amount and buy count inside the window, source URL; no `basis`) rendered as one sentence on the ad page and vendor page — "In the week before and while this ad ran, the sponsor reported digital buys to X and Y; FEC records do not identify which vendor placed this ad." The window is `[first_shown − 7 days, last_shown]` (placement is paid before air) and the copy names the lead week wherever the rule is stated. Per vendor, context and link amounts count only its placeable buys (digital / production / other), never its same-window TV or mail dollars; medium is the dominant placeable one. A `verified` link needs no dated payment (the source is the evidence): it is emitted with `buys_in_window` 0 and a null `window` when the ad has no dates. `chains_out._ad_parent_edge` and `ad-view.ts` filter to verified/inferred so a stale record can never become an edge. Supersedes D-63's adjacent tier and D-71; resolves Q-12. | Drawn as a dotted edge, co-occurrence read as a relationship the data does not support (Albert: "more confusing now to have the implied links"). The FEC row has no ad ID and Google has no vendor field, so nothing joins them; a sentence states the overlap without asserting the pair. Links 497 adjacent + 115 inferred → 115 inferred; 280 of 500 ads carry same-window context (611 rows). | master (D-74) |
| D-76 | Stance `direction` is an optional int -2..+2, human-coded against `ISSUE_AXES`; `needs_review` stays true, and absent means no coded position, so existing files still validate. Personalization is client-only: prefs stay in localStorage under `citizen-gotham:prefs:v1`, with no pipeline output, fetches, or LLM. Per-issue agreement is `1 - |user - direction| / 4`; scores are weighted means using importance and confidence; races filter by `RaceSummary.state`. The UI calls each result an alignment estimate, not guidance on how to vote, and every score links to evidence. | Keep user criteria private while making the estimate traceable to the same dossier records and published issue poles. | personalize session |
| D-77 | Chain page (Block 3 child C): the root is drawn with a thick ink outline, a filled ink name chip and a "YOU ARE HERE · THE SPENDER" caption, plus a legend entry — page ink, never a party colour or a visibility colour. Node labels are fitted by an estimated glyph-width table (`chain/label.ts`, Arial/Helvetica advances × 1.04) rather than `getComputedTextLength`, so server and client lay out identically and there is no hydration mismatch; a name that does not fit one row with its amount wraps to two rows (with the box growing to a 42px minimum) and is ellipsised beyond that, with the full name in the SVG label, the tooltip and the panel. The "N more ads" roll-up is sorted to the foot of the ads column with an extra gap. The edge table is folded behind a `Show all N edges` control; every row's `id` is `edge-<index in chain.edges>` and the diagram wire carries that index as a ninth, additive tuple slot, so a click on a drawn edge opens the fold, scrolls to its row and marks it, and also opens an edge panel (both ends, dollars with their meaning, basis rule and sources). Hover/focus tooltips are one absolutely positioned HTML element beside the pointer (or the focused element), moved without re-rendering the SVG; ad tooltips show the cached thumbnail when there is one. Zoom/pan deferred (child G). | The root was indistinguishable from a hop-1 committee at a glance; long names overflowed a fixed 210px box; the table dwarfed the picture on load; edges had no evidence path of their own. Measuring text in the client would have meant a layout that differs between SSR and hydration. | Block 3 child C |
| D-78 | Chain camera (Block 3 child H1a): the camera is a rectangle in layout units written to `<svg viewBox>` (never a CSS transform, never d3-zoom); nothing about it lives in React state — viewBox, `data-k` and the toolbar readout are all direct DOM writes, one per animation frame. Plain wheel never zooms; `Ctrl`/`⌘`+wheel, pinch, drag on empty canvas, double-click and the toolbar/keys do. `kMax` = 18 px / 11 pt labels; `kMin` = fit. `web/` tests run with `node --test` over `tsc`-compiled `src/**/*.test.ts` (`npm test`), no new dependency. | Re-rendering a 60-node SVG on each scale-bucket change dropped one frame in seven on WINSENATE; direct writes hold 60 fps. viewBox keeps text crisp at any k and leaves the server layout (D-24) untouched. Hijacking plain wheel breaks page scrolling for everyone who did not mean to zoom. A vitest/jest install would be a new dependency for one pure module. | Block 3 child H1a |
| D-79 | Race ledger "Funding overview" keeps one pie per tab (all candidates / each candidate): campaign receipts vs outside spending. A **Show detail** toggle re-cuts the same pie into five slices — receipts from individuals / from committees / other (three shades of the campaign hue) and outside spending supporting / opposing (two shades of the outside hue, D-16) — so the darker half is always money that reached the campaign and the lighter half independent expenditures that never did. Selecting a detail slice opens its group in the detail column and marks its line; the summary/detail split is `pieSlices(view, detailed)` in `funding-view.ts`, the same source URLs as before. No side-by-side per-candidate charts, no summing across candidates. | Sep 6, Patrick: "instead of having the two charts, we just want more granularity in each pie… a button that allows this more granularized view." Supersedes the two-pie "Money working for <candidate>" cut and the support/oppose pair before it (both in this PR's history). Conduit receipts stay inside the individuals slice (they are a subset, not a source); the caption says so. | Patrick + Block 3 child B |
